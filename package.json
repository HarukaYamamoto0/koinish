{
  "name": "koinish",
  "version": "1.0.0",
  "description": "A minimalist, Koin-like DI for TypeScript.",
  "license": "MIT",
  "private": false,
  "sideEffects": false,
  "types": "src/index.ts",
  "files": ["src", "README.md", "LICENSE"],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:cov": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@vitest/coverage-v8": "^4.0.8",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8",
    "reflect-metadata": "^0.1.14"
  },
  "peerDependencies": {
    "reflect-metadata": "^0.1.14"
  }
}
